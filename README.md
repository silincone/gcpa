# gcpa
Global Critical Path delay Analyzer

Parses multiple timing reports and prints the Global Critical Path Design and its corresponding Delay.

Usage: `gcpa.py [-h] files [files ...]`

```
positional arguments:
  files       List of timing files (.txt) to process.

options:
  -h, --help  show this help message and exit
```
